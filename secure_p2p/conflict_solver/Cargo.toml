[package]
name = "conflict_solver"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
ledger_core = { path = "../ledger_core" }
log = "0.4"
