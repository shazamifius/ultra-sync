[package]
name = "crash_recovery"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
ledger_core = { path = "../ledger_core" }
chunk_engine = { path = "../chunk_engine" }
log = "0.4"
